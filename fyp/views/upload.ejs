<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Uploaded!</title>
    <style>
      body {
        background-color: #fff;
        font-family: Arial, sans-serif;
        font-size: 16px;
        text-align: center;
        margin: 0;
        padding: 0;
      }
      h1 {
        font-size: 3rem;
        font-weight: bold;
        margin-top: 5rem;
        margin-bottom: 2rem;
      }
      a {
        color: #0070c0;
        text-decoration: none;
        font-weight: bold;
      }
      a:hover {
        text-decoration: underline;
      }
      table {
        border-collapse: collapse;
        margin: 3rem auto;
        width: 90%;
        max-width: 600px;
      }
      th, td {
        padding: 1rem;
        text-align: left;
        border: 1px solid #eee;
      }
      th {
        background-color: #0070c0;
        color: #fff;
      }
    </style>
  </head>
  <body>
    <h1>Upload Successful!</h1>
    <% if (files.length > 0) { %>
      <table>
        <thead>
          <tr>
            <th>File Name</th>
            <th>File Link</th>
          </tr>
        </thead>
        <tbody>
          <% for (var i = 0; i < files.length; i++) { %>
            <tr>
              <td><%= files[i].fileName %></td>
              <td><a href="<%= files[i].fileUrl %>"><%= files[i].fileHash %></a></td>
            </tr>
          <% } %>
        </tbody>
      </table>
    <% } else { %>
      <p>No files uploaded yet.</p>
    <% } %>
  </body>
</html>
